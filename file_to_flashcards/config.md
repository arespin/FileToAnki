# FileToFlashcards Configuration

## Settings

### api_key
Your Claude API key from Anthropic. Get one at https://console.anthropic.com/

### default_deck
The default deck to add new flashcards to. Must match an existing deck name in your collection.

### max_cards
Maximum number of flashcards to generate per file (1-50). Default is 25.
